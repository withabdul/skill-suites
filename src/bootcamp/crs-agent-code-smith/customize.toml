# DO NOT EDIT -- overwritten on every update.
#
# Agent customization surface for crs-agent-code-smith.
# Team overrides:     {project-root}/.ssconfig/custom/crs-agent-code-smith.toml
# Personal overrides: {project-root}/.ssconfig/custom/crs-agent-code-smith.user.toml

[agent]

# --- Metadata (install-time roster contract) ---
# Consumed by module.yaml:agents[] and `[agents.<code>]` in central config.

code = "agent-code-smith"
name = ""
title = "Course Code Smith"
icon = "🔧"
description = "Creating coding exercises, environment scaffolding, and debugging scenarios that are clean and runnable."
agent_type = "memory"