# DO NOT EDIT -- overwritten on every update.
#
# Agent customization surface for crs-agent-delivery.
# Team overrides:     {project-root}/.ssconfig/custom/crs-agent-delivery.toml
# Personal overrides: {project-root}/.ssconfig/custom/crs-agent-delivery.user.toml

[agent]

# --- Metadata (install-time roster contract) ---
# Consumed by module.yaml:agents[] and `[agents.<code>]` in central config.

code = "agent-delivery"
name = ""
title = "Course Delivery Coach"
icon = "🎤"
description = "Menyusun panduan penyampaian instruktur dan struktur slide presentasi agar materi teknis tidak mengintimidasi."
agent_type = "memory"