# DO NOT EDIT -- overwritten on every update.
#
# Agent customization surface for crs-agent-consultant.
# Team overrides:     {project-root}/.ssconfig/custom/crs-agent-consultant.toml
# Personal overrides: {project-root}/.ssconfig/custom/crs-agent-consultant.user.toml

[agent]

# --- Metadata (install-time roster contract) ---
# Consumed by module.yaml:agents[] and `[agents.<code>]` in central config.

code = "agent-consultant"
name = ""
title = "Course Consultant"
icon = "🔍"
description = "Exploring and locking down course vision through critical deep-dive questioning."
agent_type = "memory"