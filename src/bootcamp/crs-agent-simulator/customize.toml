# DO NOT EDIT -- overwritten on every update.
#
# Agent customization surface for crs-agent-simulator.
# Team overrides:     {project-root}/.ssconfig/custom/crs-agent-simulator.toml
# Personal overrides: {project-root}/.ssconfig/custom/crs-agent-simulator.user.toml

[agent]

# --- Metadata (install-time roster contract) ---
# Consumed by module.yaml:agents[] and `[agents.<code>]` in central config.

code = "agent-simulator"
name = ""
title = "Course Simulator"
icon = "🪞"
description = "Self-auditing course material quality through student simulation with various extreme personas."
agent_type = "memory"